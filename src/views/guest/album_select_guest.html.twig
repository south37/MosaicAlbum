{% extends 'layout.html.twig' %}

{% block contents %}
    <!-- Main hero unit for a primary marketing message or call to action -->
    <div class="hero-unit">
      <h1>album select guest</h1>
      <p>This is a demonstration page with GroupWorkBase.</p>
      <p><a href="{{ urlFor('doc_top') }}" class="btn btn-primary btn-large">Learn more &raquo;</a></p>
    </div>

    <a class="btn btn-primary" href="{{ urlFor('create_mosaic') }}">create mosaic</a>
    {% endblock %}
